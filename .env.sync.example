# Environment Variables for Data Sync System

# Database Connection (Required)
DATABASE_URL="postgresql://username:password@host:port/database?sslmode=require"

# Sync Authentication Secret (Required for production)
SYNC_SECRET="your-secure-random-secret-key-here"

# Development Options (Optional)
NEXT_PUBLIC_ENABLE_STARTUP_SYNC="true"

# Example Production Variables for Vercel
# Set these in your Vercel dashboard under Settings > Environment Variables

# DATABASE_URL=postgresql://your-google-cloud-sql-connection
# SYNC_SECRET=generated-secure-secret-for-api-auth
# NEXTAUTH_URL=https://kaylode.com
# NEXTAUTH_SECRET=your-nextauth-secret

# Note: The sync system will:
# 1. Check if database is available
# 2. Fetch all content from your PostgreSQL database
# 3. Write it to static files in src/data/
# 4. Enable fallback to static data if database goes offline
